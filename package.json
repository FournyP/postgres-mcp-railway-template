{
  "name": "postgres-mcp-railway-template",
  "private": true,
  "description": "Railway Infrastructure as Code for the Postgres MCP server and its auth gateway",
  "license": "MIT",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "plan": "railway config plan",
    "apply": "railway config apply"
  },
  "dependencies": {
    "railway": "^3.11.0"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "typescript": "^7.0.0"
  }
}
